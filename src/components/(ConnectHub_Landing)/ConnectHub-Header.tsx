import { FindStartConversation } from "../SidebarSearch"

export const LandingHeader = () => {
  <header className="relative h-12 flex items-center justify-between gap-1 font-semibold shadow-md
   hover:bg-[#35373c] px-4 py-3 transition-colors duration-100 ease-linear">
  </header>
}